Converts delimited file from Mobius View and converts it to a standard format. 
convert.cmd takes these arguments
 -d,--delete         Optional - Delete original input file after conversion
 -f,--format <arg>   Optional - Output format
 -i,--input <arg>    Required - Input CSV FileIf you do not pass true, the original filename gets an extension of .original. 

